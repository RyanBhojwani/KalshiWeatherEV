{
  "name": "kalshi-weather-ev",
  "version": "1.0.0",
  "private": true,
  "description": "Kalshi temperature market +EV finder with weather forecast comparison",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:worker": "npm run dev --workspace=apps/worker",
    "build": "npm run build --workspace=packages/shared && npm run build --workspace=apps/worker && npm run build --workspace=apps/web",
    "build:shared": "npm run build --workspace=packages/shared",
    "build:worker": "npm run build --workspace=apps/worker",
    "build:web": "npm run build --workspace=apps/web"
  }
}
